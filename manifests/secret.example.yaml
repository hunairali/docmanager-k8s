# Template only. Copy this file to secret.yaml, fill in real values, and never
# commit secret.yaml - it's excluded via .gitignore in this repo for exactly
# that reason.
apiVersion: v1
kind: Secret
metadata:
  name: docmanager-api-secrets
  namespace: cts-platform
type: Opaque
stringData:
  DB_USER: docmanager
  DB_PASSWORD: change-me
